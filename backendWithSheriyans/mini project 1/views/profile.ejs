<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Document</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
  <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
    <div class="w-full flex justify-end">
      <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">logout</a>
    </div>

    <div class="flex items-center gap-2">
      <div class="w-15 h-15 rounded-md object-cover overflow-hidden"><img src="/images/uploads/<%= user.profilepic %>" alt=""></div>
      <h3 class="text-3xl">
        <span class="font-light">Hello</span>
        <%= user.name %> 👋
      </h3>
    </div>

    <h5 class="mb-5">You can create a new post.</h5>

    <form action="/post" method="post">
      <textarea class="w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md p-3 outline-none"
        placeholder="what's on your mind?" name="content"></textarea>
      <input class="text-sm px-3 w-40 py-2 bg-blue-500 block rounded-md" type="submit" value="Create new post" />
    </form>

    <div class="posts mt-20">
      <h3 class="text-zinc-400">Your Posts.</h3>
      <div class="postcontainer flex flex-wrap mt-5">
        <% user.posts.reverse().forEach(function(post){ %>
          <div class="post w-1/3 p-4 mr-2 mb-2 rounded-md border-[1px] border-zinc-800">
            <h4 class="text-blue-500 mb-2">@someusername</h4>
            <p class="text-sm tracking-tight">
              <%= post.content %>
            </p>

            <small class="mt-4 inline-block">
              <%= post.likes.length %> likes
            </small>
            <div class="btns flex gap-4 mt-5">
              <a class="text-blue-500" href="/like/<%= post._id %>">
                <%= post.likes.indexOf(user._id)===-1 ? "Like" : "Unlike" %>
              </a>
              <a class="text-zinc-400" href="/edit/<%= post._id %>">edit</a>
            </div>
          </div>

          <% }); %>
      </div>
    </div>
  </div>
</body>

</html>